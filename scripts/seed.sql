-- Demo seed data (run after schema, replace github_id with real IDs if needed)
-- First user login via GitHub will auto-create workspace; this seed is optional for local demo.

-- Example: insert demo workspace tasks after users exist
-- INSERT INTO tasks (workspace_id, title, description, priority, status, created_by)
-- VALUES (1, '示例任务', '这是一个示例任务', 'high', 'todo', 1);
